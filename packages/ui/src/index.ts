// @moc/ui uses deep imports — `import { Button } from '@moc/ui/components/controls/button'`
// to mirror the existing per-file import style and avoid coupling unrelated components.
// Stylesheet is exposed at '@moc/ui/styles.css'.
export {}
