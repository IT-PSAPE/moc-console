VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_DEFAULT_KEY=

# MOC Broadcast has no auth and no baked workspace. Viewers pick a
# workspace at runtime from the chooser (anon list_signup_workspaces
# RPC), then its id is carried in the URL path.
